进入 termux 环境，环境变量似乎不对

```bash
sudo chroot /proc/1/root /system/bin/sh -c ' \
    export PATH=/system/bin:/system/xbin; \
    exec /system/bin/run-as com.termux /data/data/com.termux/files/usr/bin/login \
'
```