首先重命名

```bash
git commit --amend
```

> 默认会使用 vi, 如果没有 vi 可以使用其他的编辑器

```bash
EDITOR=nano git commit --amend
```

git 会使用新的提交替换上一个提交

如果更改之前就已经推送了 (push 失败)

```bash
# 仅当确定远程分支没有被别人更新时才强制推送
git push --force-with-lease

# 或者强制覆盖
# git push --force
```
